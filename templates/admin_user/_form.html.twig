{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.status) }}
    <button class="btn btn-primary mt-3" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
 
