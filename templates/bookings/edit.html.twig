{% extends 'base.html.twig' %}

{% block title %}Edit Bookings{% endblock %}

{% block stylesheets %}
<style>
    /* ===== COLOR SCHEME =====
     * Primary Brown: #6B5B4D
     * Dark Brown: #4A3F35
     * Light Brown: #8B7968
     * Accent Brown: #9B826F
     * Light Grey: #F5F5F5
     * Medium Grey: #E8E8E8
     * Dark Grey: #7A7A7A
     */

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #F5F5F5;
        color: #333;
    }

    /* ===== DASHBOARD CONTAINER ===== */
    .dashboard-container {
        display: flex;
        min-height: 100vh;
    }

    /* ===== SIDEBAR ===== */
    .sidebar {
        width: 260px;
        background: linear-gradient(180deg, #6B5B4D 0%, #4A3F35 100%);
        color: white;
        padding: 20px 0;
        box-shadow: 4px 0 10px rgba(0, 0, 0, 0.1);
        position: fixed;
        height: 100vh;
        overflow-y: auto;
    }

    .sidebar-logo {
        padding: 20px;
        text-align: center;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 30px;
    }

    .logo-img {
        width: 120px;
        height: auto;
        margin-bottom: 10px;
    }

    .sidebar-logo p {
        color: #E8E8E8;
        font-size: 14px;
        font-weight: 300;
        letter-spacing: 1px;
    }

    .sidebar-menu {
        list-style: none;
        padding: 0 15px;
    }

    .sidebar-menu li {
        margin-bottom: 5px;
    }

    .sidebar-menu a {
        display: flex;
        align-items: center;
        padding: 12px 15px;
        color: #E8E8E8;
        text-decoration: none;
        border-radius: 8px;
        transition: all 0.3s ease;
        font-size: 15px;
    }

    .sidebar-menu a:hover {
        background-color: rgba(255, 255, 255, 0.1);
        padding-left: 20px;
    }

    .sidebar-menu a.active {
        background-color: #9B826F;
        color: white;
        font-weight: 500;
    }

    .sidebar-menu .icon {
        font-size: 18px;
        margin-right: 12px;
        width: 24px;
        text-align: center;
    }

    /* ===== MAIN CONTENT ===== */
    .main-content {
        flex: 1;
        margin-left: 260px;
        padding: 30px;
        background-color: #F5F5F5;
    }

    .crud-container {
        max-width: 800px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .crud-header {
        background: linear-gradient(135deg, #8B6F47 0%, #6B5438 100%);
        padding: 35px 40px;
    }

    .crud-header h1 {
        font-size: 2.2rem;
        font-weight: 600;
        margin: 0;
        color: #ffffff;
    }

    .crud-content {
        padding: 40px;
    }

    form {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    form label {
        font-weight: 600;
        color: #333333;
        margin-bottom: 8px;
        display: block;
        font-size: 0.95rem;
    }

    form input[type="text"],
    form input[type="number"],
    form input[type="date"],
    form input[type="datetime-local"],
    form select,
    form textarea {
        width: 100%;
        padding: 14px 16px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 0.95rem;
        color: #333333;
        background: #ffffff;
        transition: all 0.3s ease;
        box-sizing: border-box;
    }

    form input:focus,
    form select:focus,
    form textarea:focus {
        outline: none;
        border-color: #8B6F47;
        box-shadow: 0 0 0 3px rgba(139, 111, 71, 0.1);
        background: rgba(139, 111, 71, 0.02);
    }

    textarea {
        resize: vertical;
        min-height: 100px;
    }

    .button-group {
        display: flex;
        gap: 15px;
        flex-wrap: wrap;
        margin-top: 25px;
    }

    .btn {
        padding: 14px 32px;
        border: none;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
    }

    .btn-update {
        background: linear-gradient(135deg, #8B6F47 0%, #6B5438 100%);
        color: #ffffff;
    }

    .btn-update:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(139, 111, 71, 0.3);
    }

    .btn-cancel {
        background: #ececec;
        color: #333333;
        border: 2px solid #e0e0e0;
    }

    .btn-cancel:hover {
        background: #e0e0e0;
        transform: translateY(-2px);
    }

    .btn-delete {
        background: #c0392b;
        color: #ffffff;
    }

    .btn-delete:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(192, 57, 43, 0.3);
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
        .sidebar {
            width: 70px;
        }

        .main-content {
            margin-left: 70px;
            padding: 15px;
        }

        .sidebar-logo p,
        .sidebar-menu a span:not(.icon) {
            display: none;
        }

        .sidebar-menu a {
            justify-content: center;
            padding: 12px;
        }

        .crud-content {
            padding: 25px;
        }

        .crud-header {
            padding: 25px;
        }

        .crud-header h1 {
            font-size: 1.8rem;
        }

        .button-group {
            flex-direction: column;
        }

        .btn {
            width: 100%;
            text-align: center;
        }
    }
</style>
{% endblock %}

{% block body %}
<div class="dashboard-container">
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-logo">
            <img src="/images/logo_white.png" alt="Reservue Logo" class="logo-img">
            <p>Admin Dashboard</p>
        </div>

        <ul class="sidebar-menu">
            <li><a href="{{ path('app_dashboard') }}"><span class="icon">📊</span>Dashboard</a></li>
            <li><a href="{{ path('app_product_index') }}"><span class="icon">📦</span>Product Inventory</a></li>
            <li><a href="{{ path('app_bookings_index') }}" class="active"><span class="icon">🎫</span>Bookings</a></li>
            <li><a href="{{ path('app_category_index') }}"><span class="icon">📁</span>Categories</a></li>
        </ul>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div class="crud-container">
            <div class="crud-header">
                <h1>Edit Booking</h1>
            </div>

            <div class="crud-content">
                {{ include('bookings/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="button-group">
                    <a href="{{ path('app_bookings_index') }}" class="btn btn-cancel">Back to List</a>

                    {# Delete form included as a button #}
                    <form method="post" action="{{ path('app_bookings_delete', {'id': booking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this booking?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">
                        <button class="btn btn-delete">Delete Booking</button>
                    </form>
                </div>
            </div>
        </div>
    </main>
</div>
{% endblock %}