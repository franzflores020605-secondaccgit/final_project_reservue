{% extends 'dashboard/base.html.twig' %}

{% block title %}New Traveler{% endblock %}

{% block dashboard_body %}
        <div class="product-container">
            <div class="product-header">
                <h1>Create New Traveler</h1>
                <div class="header-buttons">
                    <a href="{{ path('app_traveler_index') }}" class="btn btn-secondary" data-turbo-frame="reservue-dashboard-outlet">← Back to List</a>
                </div>
            </div>

            <div class="table-wrapper">
                {{ include('traveler/_form.html.twig') }}
            </div>
        </div>
{% endblock %}
